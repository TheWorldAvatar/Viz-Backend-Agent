version: "3.9"

services:
  agent:
    container_name: vis-backend-agent
    image: ghcr.io/theworldavatar/vis-backend-agent:1.33.4-white-space-search-SNAPSHOT
    build:
      context: ..
      target: agent
    environment:
      NAMESPACE: kb
    volumes:
      - ../resources/config:/usr/local/tomcat/resources
    ports:
      - "8080:8080"
