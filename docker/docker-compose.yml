version: "3.9"

services:
  agent:
    container_name: vis-backend-agent
    image: ghcr.io/theworldavatar/vis-backend-agent:1.45.1-invalid-delete-for-number-literals-SNAPSHOT
    build:
      context: ..
      target: agent
    environment:
      NAMESPACE: kb
      SHACL_NAMESPACE: shacl
    volumes:
      - ../resources/config:/usr/local/tomcat/resources
    ports:
      - "8080:8080"
