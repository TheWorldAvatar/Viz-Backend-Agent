version: "3.9"

services:
  agent:
    container_name: vis-backend-agent-test
    image: ghcr.io/theworldavatar/vis-backend-agent-test:1.68.0-batch-shacl-rule-SNAPSHOT
    build:
      context: ..
      target: test
    environment:
      NAMESPACE: kb
      SHACL_NAMESPACE: shacl
    ports:
      - "8080:8080"
