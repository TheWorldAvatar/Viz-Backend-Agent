version: "3.9"

services:
  agent:
    container_name: vis-backend-agent-test
    image: ghcr.io/theworldavatar/vis-backend-agent-test:1.62.0-closed-task-speed-hotfix-SNAPSHOT
    build:
      context: ..
      target: test
    environment:
      NAMESPACE: kb
      SHACL_NAMESPACE: shacl
    ports:
      - "8080:8080"
